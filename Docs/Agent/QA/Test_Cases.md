# 测试用例

最后更新：2026-05-10

## TC-01 怪物 AI C++ 编译

- 前置条件：UE 5.4 构建工具可用。
- 操作：构建 `Combat_Agent_DemoEditor Win64 Development`。
- 预期：新增怪物 AI 数据资产、组件、角色和日志分类通过 UHT 与 C++ 编译。
- 实际：编译通过。
- 结果：通过。

## TC-02 招式评分基础选择

- 前置条件：创建怪物数据资产，配置至少两个不同距离的招式。
- 操作：构建评分上下文并调用 `SelectAttackMove`。
- 预期：组件输出所有招式评分，并选择最高分且可执行的招式。
- 实际：未运行。
- 结果：待测。

## TC-03 招式失败回退

- 前置条件：最高分招式冷却未结束或距离不匹配。
- 操作：调用 `SelectAttackMove`。
- 预期：返回失败，记录中文失败原因，行为树回退到移动、转向或等待。
- 实际：未运行。
- 结果：待测。

## TC-04 仇恨目标切换

- 前置条件：怪物 AI 组件存在两个候选目标。
- 操作：对第二个目标增加超过阈值的仇恨。
- 预期：当前目标切换到更高仇恨目标，并输出日志。
- 实际：未运行。
- 结果：待测。

## TC-05 部位破坏

- 前置条件：数据资产配置至少一个可破坏部位。
- 操作：调用 `ApplyPartDamage` 直到部位耐久低于破坏阈值。
- 预期：部位状态变为破坏，记录最近破坏部位。
- 实际：未运行。
- 结果：待测。

## TC-06 部位硬直

- 前置条件：数据资产配置部位硬直阈值。
- 操作：持续对同一部位施加伤害。
- 预期：达到硬直阈值后 `bStaggerReady` 为真，并记录最近硬直部位。
- 实际：未运行。
- 结果：待测。

## TC-07 行为树评分接入

- 前置条件：黑板和行为树资产已创建，怪物蓝图继承 `AMonsterCharacter` 或挂载组件。
- 操作：行为树组装评分上下文并调用战斗组件。
- 预期：黑板写入 `SelectedMoveId`、评分摘要和失败原因。
- 实际：未运行。
- 结果：待测。

## TC-08 编辑器播放测试

- 前置条件：数据资产、行为树、黑板、EQS 和怪物蓝图配置完成。
- 操作：运行编辑器播放测试。
- 预期：日志出现状态、目标、招式、评分、EQS 摘要和失败回退。
- 实际：未运行。
- 结果：待测。

## TC-09 目录重构编译验证

- 前置条件：源码和文档已移动到新目录。
- 操作：构建 `Combat_Agent_DemoEditor Win64 Development`。
- 预期：UHT 和 C++ 编译器能识别新路径，所有模块链接通过。
- 实际：首次失败于 include 根路径缺失；在构建规则中加入模块根 include 后编译通过。
- 结果：通过。

## TC-10 行为树任务编译验证

- 前置条件：新增怪物 AI 行为树任务 C++ 类。
- 操作：构建 `Combat_Agent_DemoEditor Win64 Development`。
- 预期：UHT 能生成行为树任务反射代码，C++ 编译和链接通过。
- 实际：`BTTask_MonsterSelectAttackMove`、`BTTask_MonsterBuildMoveIntent`、`BTTask_MonsterUpdateDebugSnapshot` 均编译通过。
- 结果：通过。

## TC-11 行为树招式评分运行时验证

- 前置条件：编辑器中创建黑板、行为树、怪物数据资产和怪物蓝图。
- 操作：在行为树中添加 `MonsterSelectAttackMove` 并配置黑板键。
- 预期：黑板写入选中招式、失败原因和评分摘要，日志出现评分结果。
- 实际：未运行。
- 结果：待测。

## TC-12 怪物 AIController 与黑板服务编译验证

- 前置条件：新增 `AMonsterAIController` 和 `BTService_MonsterUpdateCombatBlackboard`。
- 操作：构建 `Combat_Agent_DemoEditor Win64 Development`。
- 预期：控制器、服务和怪物角色默认 AI 设置通过 UHT、C++ 编译和链接。
- 实际：编译通过。
- 结果：通过。

## TC-13 黑板刷新服务运行时验证

- 前置条件：行为树资产挂载 `MonsterUpdateCombatBlackboard` 服务，并配置黑板键。
- 操作：运行编辑器播放测试，让怪物拥有有效目标。
- 预期：黑板持续更新目标距离、目标角度、玩家数量和状态布尔值。
- 实际：未运行。
- 结果：待测。

## TC-14 黑板默认键名编译验证

- 前置条件：新增 `MonsterBlackboardKeys.h` 并在行为树任务和服务构造函数中设置默认键名。
- 操作：构建 `Combat_Agent_DemoEditor Win64 Development`。
- 预期：黑板键名常量和默认 `FBlackboardKeySelector` 设置通过编译。
- 实际：编译通过。
- 结果：通过。

## TC-15 黑板默认键名编辑器验证

- 前置条件：编辑器中创建同名黑板键。
- 操作：把怪物行为树任务和服务加入行为树资产。
- 预期：节点默认键名匹配黑板资产，无需逐个手动修改。
- 实际：未运行。
- 结果：待测。
